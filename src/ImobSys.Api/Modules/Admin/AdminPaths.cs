namespace ImobSys.Api.Modules.Admin;

public static class AdminPaths

{
    private const string Base = "/admin";
    public const string Properties = $"{Base}/properties";
    public const string PropertiesTypes = $"{Base}/property-types";
}
